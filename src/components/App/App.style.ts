import {styled} from '@material-ui/styles';

const div = styled('div');

export const FloatingExampleContent = div({
	padding: 10,
	border: '1px solid black',
	backgroundColor: 'white',
	borderRadius: 4,
	boxShadow: '0.4em 0.4em 5px rgba(122, 122, 122, 0.2)'
});